#include "main.h"
#include <stdio.h>

/**
 * main - test function
 *
 * Return: 0
 */
int main(void)
{
	short int i = SHRT_MAX;
	int p = 0;
	int len, len1;

	len = printf("%hd", i);
	putchar(10);
	len1 = write_int_short(i, &p);
	putchar(10);
	printf("%d %d\n", len, len1);
	return (0);
}
